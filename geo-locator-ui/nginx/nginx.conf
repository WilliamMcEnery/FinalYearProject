server {

  listen 80;

  location / {
    root   /usr/share/nginx/html/;
    index index.html;
    try_files $uri /index.html;
  }

  location /api {
    client_max_body_size 10m;
    proxy_pass http://geo-locator-service:8080/;
  }
}
